const redisClient = require('./redis-cli');


const saveRedis = async () => {
    return new Promise((resovle, reject) => {
        
    })
}
const search = async () => {
    return new Promise((resovle, reject) => {

    })
}

module.exports = {
    saveRedis,
    search
}